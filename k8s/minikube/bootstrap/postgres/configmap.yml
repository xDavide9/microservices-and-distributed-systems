apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
data:
    POSTGRES_DB: "amigoscode"
    POSTGRES_USER: "amigoscode"
    POSTGRES_PASSWORD: "password"
# a configmap is used to store non-sensitive data that cab be used by pods in the cluster (this case is just for demonstration, user and password
# should be stored in a secret)
